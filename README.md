# segment-01
A simple game using the SFML library.

## Coverage

```
gcovr -r . --txt-metric branch --filter '(src|include)/.*' --decisions --exclude-throw-branches --exclude '.*_deps/.*' --html --html-details -o coverage/coverage.html
```
